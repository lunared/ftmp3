version: '2'
services:
  site:
    build: .
    expose:
     - "5000"
    volumes:
     - ./:/var/www:ro
     - ./music:/media/Music
  nginx:
    image: nginx:alpine
    volumes:
     - ./nginx.conf:/etc/nginx/nginx.conf:ro
     - ./music:/media/Music
    ports:
     - "5000:80"
